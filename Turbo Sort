WRITTEN BY - KUNWAR YASHASWWEE CHHAUNKER

PROBLEM - 

Given the list of numbers, you are to sort them in non decreasing order.
Input
t – the number of numbers in list, then t lines follow [t <= 10^6].
Each line contains one integer: N [0 <= N <= 10^6]

Output
Output given numbers in non decreasing order.
Example
Input:

5
5
3
6
7
1
Output:

1
3
5
6
7

SOLUTION - 

WRITTEN BY -  KUNWAR YASHASWEE CHHAUNKER

#include<bits/stdc++.h>
using namespace std;
int main(){
    int number, i;
    cin>>number;
    int arr[number];
    for(i = 0 ; i < number ; i++){
        cin>>arr[i];
    }
    sort(arr, arr + number);
    for( i = 0 ; i < number ; i++){
        cout<<arr[i]<<endl;
    }
    return 0;
}
